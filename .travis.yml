os: osx
osx_image: xcode9.1
language: go

go:
  - 1.9
go_import_path: github.com/dmacvicar/terraform-provider-libvirt
install: true
before_script:
  - brew upgrade
  - launchctl list
  - launchctl load -w /System/Library/LaunchDaemons/libvirtd.plist
  - brew install qemu
  - brew install libvirt
script:
  - bash travis/run-tests-inside-guest
